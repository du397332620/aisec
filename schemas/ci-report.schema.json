{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aisec.dev/schemas/ci-report-1.3.0.json",
  "title": "AIsec CiReport 1.3.0",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "toolVersion",
    "scanId",
    "profileName",
    "decision",
    "recommendedExitCode",
    "decisionReasons",
    "counts",
    "requiredCoverage",
    "policy",
    "annotations",
    "omitted",
    "disclaimer"
  ],
  "properties": {
    "schemaVersion": { "enum": ["1.0.0", "1.1.0", "1.2.0", "1.3.0"] },
    "toolVersion": { "$ref": "#/$defs/semver" },
    "scanId": { "$ref": "#/$defs/scanId" },
    "profileName": { "enum": ["predeploy", "native"] },
    "decision": { "$ref": "#/$defs/decision" },
    "recommendedExitCode": { "enum": [0, 1, 2] },
    "decisionReasons": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "items": { "$ref": "#/$defs/reason" }
    },
    "counts": { "$ref": "#/$defs/counts" },
    "requiredCoverage": { "$ref": "#/$defs/requiredCoverage" },
    "policy": { "$ref": "#/$defs/policy" },
    "rulePacks": {
      "type": "array",
      "maxItems": 8,
      "items": { "$ref": "#/$defs/rulePackRecord" }
    },
    "routeAttribution": { "$ref": "#/$defs/routeAttribution" },
    "comparison": { "$ref": "#/$defs/comparison" },
    "annotations": {
      "type": "array",
      "minItems": 1,
      "maxItems": 71,
      "items": { "$ref": "#/$defs/annotation" }
    },
    "omitted": {
      "type": "object",
      "additionalProperties": false,
      "required": ["coverageAnnotations", "findingAnnotations"],
      "properties": {
        "coverageAnnotations": { "type": "integer", "minimum": 0, "maximum": 100000 },
        "findingAnnotations": { "type": "integer", "minimum": 0, "maximum": 100000 }
      }
    },
    "disclaimer": { "type": "string", "minLength": 1, "maxLength": 2000 }
  },
  "allOf": [
    {
      "if": {
        "properties": { "schemaVersion": { "enum": ["1.1.0", "1.2.0", "1.3.0"] } },
        "required": ["schemaVersion"]
      },
      "then": {
        "properties": {
          "rulePacks": {
            "type": "array",
            "maxItems": 8,
            "items": { "$ref": "#/$defs/rulePackRecord" }
          }
        },
        "required": ["rulePacks"]
      }
    },
    {
      "if": {
        "properties": { "schemaVersion": { "enum": ["1.2.0", "1.3.0"] } },
        "required": ["schemaVersion"]
      },
      "then": {
        "properties": { "routeAttribution": { "$ref": "#/$defs/routeAttribution" } },
        "required": ["routeAttribution"]
      }
    },
    {
      "if": {
        "properties": { "schemaVersion": { "const": "1.1.0" } },
        "required": ["schemaVersion"]
      },
      "then": { "properties": { "routeAttribution": false } }
    },
    {
      "if": {
        "properties": { "schemaVersion": { "enum": ["1.0.0", "1.1.0", "1.2.0"] } },
        "required": ["schemaVersion"]
      },
      "then": {
        "properties": {
          "comparison": {
            "type": "object",
            "properties": { "routeSecurity": false }
          }
        }
      }
    },
    {
      "if": {
        "properties": { "schemaVersion": { "const": "1.3.0" } },
        "required": ["schemaVersion", "comparison"]
      },
      "then": {
        "properties": {
          "comparison": {
            "type": "object",
            "properties": { "routeSecurity": true },
            "required": ["routeSecurity"]
          }
        }
      }
    },
    {
      "if": {
        "properties": { "schemaVersion": { "const": "1.0.0" } },
        "required": ["schemaVersion"]
      },
      "then": { "properties": { "rulePacks": false, "routeAttribution": false } }
    }
  ],
  "$defs": {
    "semver": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?(?:\\+[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?$"
    },
    "scanId": {
      "type": "string",
      "pattern": "^scan_[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
    },
    "findingId": { "type": "string", "pattern": "^finding_[0-9a-f]{16}$" },
    "fingerprint": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "rulePackRecord": {
      "type": "object",
      "additionalProperties": false,
      "required": ["packId", "digestSha256", "ruleCount"],
      "properties": {
        "packId": {
          "type": "string",
          "minLength": 1,
          "maxLength": 80,
          "pattern": "^[a-z0-9]+(?:[.-][a-z0-9]+)*$"
        },
        "digestSha256": { "$ref": "#/$defs/fingerprint" },
        "ruleCount": { "type": "integer", "minimum": 1, "maximum": 100 }
      }
    },
    "decision": { "enum": ["block", "incomplete", "review", "no_blockers_found"] },
    "severity": { "enum": ["critical", "high", "medium", "low", "info"] },
    "evidenceLevel": { "enum": ["verified", "static_confirmed", "inferred"] },
    "reason": { "type": "string", "minLength": 1, "maxLength": 1000 },
    "counts": {
      "type": "object",
      "additionalProperties": false,
      "required": ["critical", "high", "medium", "low", "info", "open", "attackPaths", "suppressed"],
      "properties": {
        "critical": { "type": "integer", "minimum": 0 },
        "high": { "type": "integer", "minimum": 0 },
        "medium": { "type": "integer", "minimum": 0 },
        "low": { "type": "integer", "minimum": 0 },
        "info": { "type": "integer", "minimum": 0 },
        "open": { "type": "integer", "minimum": 0 },
        "attackPaths": { "type": "integer", "minimum": 0 },
        "suppressed": { "type": "integer", "minimum": 0 }
      }
    },
    "coverageGap": {
      "type": "object",
      "additionalProperties": false,
      "required": ["domain", "engine", "status"],
      "properties": {
        "domain": { "type": "string", "minLength": 1, "maxLength": 200 },
        "engine": { "type": "string", "minLength": 1, "maxLength": 200 },
        "status": { "enum": ["partial", "not_run", "failed"] },
        "reason": { "$ref": "#/$defs/reason" }
      }
    },
    "requiredCoverage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["total", "complete", "gaps"],
      "properties": {
        "total": { "type": "integer", "minimum": 0, "maximum": 1000 },
        "complete": { "type": "integer", "minimum": 0, "maximum": 1000 },
        "gaps": {
          "type": "array",
          "maxItems": 1000,
          "items": { "$ref": "#/$defs/coverageGap" }
        }
      }
    },
    "policyGate": {
      "type": "object",
      "additionalProperties": false,
      "required": ["minimumSeverity", "includeInferred", "requireNoSuppressions"],
      "properties": {
        "minimumSeverity": { "enum": ["high", "medium", "low", "info"] },
        "includeInferred": { "type": "boolean" },
        "requireNoSuppressions": { "type": "boolean" }
      }
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "source",
        "targetConfiguration",
        "requiredEngines",
        "suppressionCount",
        "suppressionApproval",
        "relaxations"
      ],
      "properties": {
        "source": { "enum": ["defaults", "operator", "not_recorded"] },
        "targetConfiguration": { "enum": ["absent", "ignored", "not_recorded"] },
        "policyId": { "type": "string", "minLength": 1, "maxLength": 128 },
        "digestSha256": { "$ref": "#/$defs/fingerprint" },
        "expiresAt": { "type": "string", "format": "date-time" },
        "gate": { "$ref": "#/$defs/policyGate" },
        "requiredEngines": {
          "type": "array",
          "maxItems": 3,
          "uniqueItems": true,
          "items": { "enum": ["gitleaks", "opengrep", "trivy"] }
        },
        "suppressionCount": { "type": "integer", "minimum": 0, "maximum": 1000 },
        "suppressionApproval": { "enum": ["not_applicable", "explicit", "not_recorded"] },
        "relaxations": {
          "type": "array",
          "maxItems": 2,
          "uniqueItems": true,
          "items": { "enum": ["source_only_profile", "external_engines_disabled"] }
        }
      }
    },
    "comparison": {
      "type": "object",
      "additionalProperties": false,
      "required": ["baselineScanId", "new", "remaining", "resolved", "notRechecked"],
      "properties": {
        "baselineScanId": { "$ref": "#/$defs/scanId" },
        "new": { "type": "integer", "minimum": 0 },
        "remaining": { "type": "integer", "minimum": 0 },
        "resolved": { "type": "integer", "minimum": 0 },
        "notRechecked": { "type": "integer", "minimum": 0 },
        "routeSecurity": { "$ref": "#/$defs/routeSecurityComparison" }
      }
    },
    "routeSecurityEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["state", "framework", "route", "category", "severity"],
      "properties": {
        "state": { "enum": ["new", "remaining", "resolved", "not_rechecked"] },
        "framework": { "enum": ["FastAPI", "Express", "NestJS"] },
        "route": {
          "type": "string",
          "minLength": 5,
          "maxLength": 512,
          "pattern": "^(CONNECT|DELETE|GET|HEAD|OPTIONS|PATCH|POST|PUT|TRACE|ALL) /[^\\s\\u0000-\\u001f\\u007f]{0,480}$"
        },
        "category": {
          "enum": [
            "authentication",
            "object_authorization",
            "privileged_authorization",
            "sql_injection",
            "ssrf",
            "untrusted_file_path",
            "credential_forwarding",
            "exception_disclosure"
          ]
        },
        "severity": { "$ref": "#/$defs/severity" }
      }
    },
    "routeSecurityComparison": {
      "type": "object",
      "additionalProperties": false,
      "required": ["recorded", "complete", "new", "remaining", "resolved", "notRechecked", "omittedRouteAliases", "omittedAssociations", "entries", "omittedEntries"],
      "properties": {
        "recorded": { "type": "boolean" },
        "complete": { "type": "boolean" },
        "new": { "type": "integer", "minimum": 0, "maximum": 10000 },
        "remaining": { "type": "integer", "minimum": 0, "maximum": 10000 },
        "resolved": { "type": "integer", "minimum": 0, "maximum": 10000 },
        "notRechecked": { "type": "integer", "minimum": 0, "maximum": 20000 },
        "omittedRouteAliases": { "type": "integer", "minimum": 0, "maximum": 1000000 },
        "omittedAssociations": { "type": "integer", "minimum": 0, "maximum": 1000000 },
        "entries": {
          "type": "array",
          "maxItems": 200,
          "items": { "$ref": "#/$defs/routeSecurityEntry" }
        },
        "omittedEntries": { "type": "integer", "minimum": 0, "maximum": 50000 }
      }
    },
    "routeAttributionReasonSummary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["reason", "signals"],
      "properties": {
        "reason": {
          "enum": [
            "commented_out_call",
            "ambiguous_or_dynamic_dispatch",
            "request_origin_not_proven",
            "no_proven_route_path",
            "not_recorded"
          ]
        },
        "signals": { "type": "integer", "minimum": 1, "maximum": 100000 }
      }
    },
    "routeAttribution": {
      "type": "object",
      "additionalProperties": false,
      "required": ["eligibleSignals", "attributedSignals", "unattributedSignals", "unattributedFindings", "reasons"],
      "properties": {
        "eligibleSignals": { "type": "integer", "minimum": 0, "maximum": 100000 },
        "attributedSignals": { "type": "integer", "minimum": 0, "maximum": 100000 },
        "unattributedSignals": { "type": "integer", "minimum": 0, "maximum": 100000 },
        "unattributedFindings": { "type": "integer", "minimum": 0, "maximum": 100000 },
        "reasons": {
          "type": "array",
          "maxItems": 5,
          "items": { "$ref": "#/$defs/routeAttributionReasonSummary" }
        }
      }
    },
    "safePath": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "pattern": "^[^\\u0000-\\u001f\\u007f]+$"
    },
    "annotation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "level", "title", "message"],
      "properties": {
        "kind": { "enum": ["decision", "coverage", "finding"] },
        "level": { "enum": ["error", "warning", "notice"] },
        "title": { "type": "string", "minLength": 1, "maxLength": 200 },
        "message": { "type": "string", "minLength": 1, "maxLength": 2000 },
        "path": { "$ref": "#/$defs/safePath" },
        "startLine": { "type": "integer", "minimum": 1 },
        "startColumn": { "type": "integer", "minimum": 1 },
        "endLine": { "type": "integer", "minimum": 1 },
        "endColumn": { "type": "integer", "minimum": 1 },
        "findingId": { "$ref": "#/$defs/findingId" },
        "fingerprint": { "$ref": "#/$defs/fingerprint" },
        "severity": { "$ref": "#/$defs/severity" },
        "evidenceLevel": { "$ref": "#/$defs/evidenceLevel" },
        "findingStatus": { "enum": ["open", "suppressed"] },
        "blocksRelease": { "type": "boolean" },
        "baselineState": { "enum": ["new", "unchanged"] }
      },
      "allOf": [
        {
          "if": { "properties": { "kind": { "const": "finding" } }, "required": ["kind"] },
          "then": {
            "properties": {
              "findingId": true,
              "fingerprint": true,
              "severity": true,
              "evidenceLevel": true,
              "findingStatus": true,
              "blocksRelease": true
            },
            "required": ["findingId", "fingerprint", "severity", "evidenceLevel", "findingStatus", "blocksRelease"]
          }
        }
      ],
      "dependentRequired": {
        "startLine": ["path"],
        "startColumn": ["path", "startLine"],
        "endLine": ["path", "startLine"],
        "endColumn": ["path", "startLine", "startColumn"]
      }
    }
  }
}
